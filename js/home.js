const $=x=>document.querySelector(x);let i=0,busy=false;const arab=n=>String(n).padStart(2,"0").replace(/\d/g,d=>"٠١٢٣٤٥٦٧٨٩"[d]);function render(){let b=books[i],r=books[(i+1)%books.length],l=books[(i-1+books.length)%books.length];$("#backdrop").style.backgroundImage=`url(${b.cover})`;$("#coverImg").src=b.cover;$("#cover").href=`book.html?id=${b.id}`;$("#title").textContent=b.title;$("#author").textContent=b.author;$("#author").href=`book.html?id=${b.id}`;$("#desc").textContent=b.desc;$("#cat").textContent=`${b.cat} · ${b.year}`;$("#download").href=b.file;$("#details").href=`book.html?id=${b.id}`;$("#num").textContent=arab(i+1);$("#peekR").innerHTML=`<img src="${r.cover}">`;$("#peekL").innerHTML=`<img src="${l.cover}">`;}function go(d){if(busy)return;busy=true;i=(i+d+books.length)%books.length;render();setTimeout(()=>busy=false,500)}$("#next").onclick=()=>go(1);$("#prev").onclick=()=>go(-1);document.onkeydown=e=>{if(e.key==="ArrowRight")go(-1);if(e.key==="ArrowLeft")go(1)};let sx;document.querySelector(".stage").ontouchstart=e=>sx=e.changedTouches[0].screenX;document.querySelector(".stage").ontouchend=e=>{let d=e.changedTouches[0].screenX-sx;if(Math.abs(d)>40)go(d>0?-1:1)};$("#row").innerHTML=books.map(b=>`<a class="card" href="book.html?id=${b.id}"><div><img src="${b.cover}"></div><label>${b.cat}</label><h3>${b.title}</h3><p>${b.author}</p></a>`).join("");render();