const grid=document.querySelector("#grid"),q=document.querySelector("#q"),f=document.querySelector("#filter");[...new Set(books.map(x=>x.cat))].forEach(x=>f.insertAdjacentHTML("beforeend",`<option>${x}</option>`));function render(){let s=q.value.toLowerCase(),c=f.value;grid.innerHTML=books.filter(b=>(c==="all"||b.cat===c)&&`${b.title} ${b.author}`.toLowerCase().includes(s)).map(b=>`<article><a class="libcover" href="book.html?id=${b.id}"><img src="${b.cover}"></a><label>${b.cat}</label><h2><a href="book.html?id=${b.id}">${b.title}</a></h2><p>${b.author}</p><a class="tiny" href="${b.file}" download>تحميل</a></article>`).join("")}q.oninput=render;f.onchange=render;render();